import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "The Aakar Theory - Interior Design Studio",
  description:
    "Expert interior design services for residential and commercial spaces. Transform your vision into reality.",
  keywords:
    "interior design, home design, commercial spaces, design studio, Rajasthan",
  openGraph: {
    title: "The Aakar Theory - Interior Design Studio",
    description:
      "Expert interior design services for residential and commercial spaces.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
